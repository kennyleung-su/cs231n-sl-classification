name = string
description = string
mode = string
arch = string
dataloader_type = string

gesture_labels = int_list

shuffle = boolean
max_example_per_label = integer(min=1, max=1000)
batch_size = integer(min=1, max=100)
epochs = integer(min=1, max=1000)
learning_rate = float(min=1e-10, max=1e5)
weight_decay = float(min=0., max=1e2)
dropout = float(min=0., max=1.)
optimizer = string
initializer = string
loss = string

resnet_num_layers = integer
freeze = boolean

lstm_hidden_size = integer(min=32, max=1024)
lstm_hidden_fc_size = integer(min=32, max=1024)
lstm_num_layers = integer(min=1, max=10)
lstm_bias = boolean
lstm_batch_first = boolean
lstm_bidirectional = boolean
max_seq_len = integer

debug = boolean
use_cuda = boolean
verbose = boolean
log_interval = integer
num_workers = integer

load = boolean
checkpoint_to_load = string